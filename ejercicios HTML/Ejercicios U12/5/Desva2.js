var objeto = document.getElementById("objeto");

// Agrega un listener de evento al objeto para el click
objeto.addEventListener("click", function() {
  objeto.style.opacity = "0"; // Cambia la opacidad del objeto a 0
});